"ProxyKiller"
{
    // There are other providers, but they may require paid subscriptions due to request limits.
    // For now, these two should work
    "blackbox.ipinfo.app"
    {
        "method"    "GET"
        "url"       "https://blackbox.ipinfo.app/lookup/{ip}"
        "response"
        {
            "type"      "plaintext"
            "value"     "Y"
        }
    }
}
